<html>
  <head>
    <title>{{title}}</title>
  <head>
  <body>
    Hi there, welcome to config modifier.
  <body>
        <h1>{{title}}</h1>
          <ul>
          {{#each children}}
            <li><a class="child" href="{{link}}">{{desc}}</a></li>
          {{/each}}
          </ul>

          value: {{data}}
        </div>
      </div>

  </body>
</html>
